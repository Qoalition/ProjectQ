import React from 'react';
import PropTypes from 'prop-types';

// Components
import Header from '../components/header/header'

const Feed = props => {
    return (
        <div>
            <Header />
        </div>
    );
};

Feed.propTypes = {
    
};

export default Feed;